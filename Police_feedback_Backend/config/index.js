const constData = require("./const-data");
const database = require("./database");
const properties = require("./properties");
const messages = require("./const-message");
const email = require("./const-mail");

module.exports = {
  constData,
  database,
  properties,
  messages,
  email
};
